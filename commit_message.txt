chore: fix investor pilot run preparations

- Replaced `lovable-agent-playwright-config` with a standard `playwright.config.ts`.
- Disabled the `react-refresh/only-export-components` ESLint rule to suppress fast refresh warnings on UI components (common for shadcn/ui components).
- Added an initial Playwright test to verify the app runs.
- Updated `browserslist` db by running `npm update-browserslist-db@latest` via `bun install caniuse-lite@latest browserslist@latest` to fix build warnings.
- Ran pre-commit checks, type-checking, and tests successfully.
- Made sure `.gitignore` ignores playwright reports.
